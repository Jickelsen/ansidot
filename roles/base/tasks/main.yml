---
###############################################################################
# Base system

- name: Base | Installs packages | apt
  become: yes
  become_user: root
  loop: "{{ base_packages }}"
  package:
    name: "{{ item }}"
    state: present
    update_cache: yes
